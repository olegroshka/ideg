# Session 2026-08-11 — AR-010

- target: AR-010 (Track E3 toy-model implementation and runs)
- mode: EXPERIMENT
- substrate versions at load: KB-001 v0.3, KB-002 v0.2, KB-003 v0.3, KB-004 v0.2, KB-005 v0.5
- substrate versions at close: (in progress)
- infrastructure notes: Python 3.11.14 (spec §6.2 said 3.12 — non-substantive,
  recorded), numpy 2.3.5, scipy 1.15.3, Windows 11 workstation.

## Plan (per ar/AR-009_spec.md, frozen; licensing recorded KB-005 §21)

1. Implement `src/ideg/`: models, states, evolution, MI-graph Φ, witnesses
   W1–W5, comparators/protocols, statistics. Unit tests first-class.
2. Run §6.3 class-label sanity checks BEFORE any pilot/confirmatory run.
3. Fix seeds in `results/AR-010/manifest.json`, commit, THEN run the
   §5.2.1 calibration pilot (exploratory; grids λ ∈ {0.02, 0.05, 0.1, 0.2},
   γ ∈ {0.003, 0.01, 0.03}; 5 runs/class; N = 10; T-A + T-C).
4. Pilot outputs → confirmatory strengths chosen → spec §8 dated entry
   (next step, not this session's runs).

Implementation clarifications adopted (candidates for a clarifying spec
amendment; none changes a preregistered threshold):

- δΦ for perturbed runs is measured against the UNPERTURBED run's window
  mean D̄_ref (the reference geometry), so log ρ compares departure from the
  same baseline.
- Dephasing channel implemented exactly per step via the Hamming damping
  mask e^(−2γΔt·d_H) between unitary steps (Trotter splitting H ⊕
  dephasing, step 0.5; exploratory pilot use).
- W1 Bohr measure includes the m = n weight at ω = 0 so the class-(i)
  prediction PR_A = 1 is well-defined (spec §5.1 states this prediction;
  the §3 formula alone would give an empty measure for an eigenstate).
- T-B is excluded from the pilot (its robustness instrument is the
  rigidity curve h_sub(ε), measured in the confirmatory phase; λ/γ
  calibration transfers from T-A/T-C). Recorded in manifest.

## Outcome

(in progress — updated as phases complete)

**Phase 1 — implementation (done).** `src/ideg/` implemented per spec:
pauli/models/states/evolve/migraph/witnesses/protocols/stats. 19 unit tests
pass, including the §4.4 null-comparator silence requirement (class (i)
silences W1/W2/W4 and freezes Φ to 1e-9), DTC perfect-flip subharmonic and
its no-interaction fragility, mixed-vs-pure RDM equality, GOE/Poisson
r-statistic references, dephasing trace/hermiticity preservation.
Performance note: mixed-state pair RDMs via cached index gathers (3 ms per
mixed Φ sample at N = 10), making the dephasing protocol tractable.

**Phase 2 — §6.3 sanity checks (ALL PASS, one pre-run amendment).**
- chaotic (mixed-field Ising, reflection-even sector, N = 10):
  ⟨r⟩ = 0.5295 ∈ [0.51, 0.55] — PASS;
- integrable (XX): preregistered Poisson window FAILED (⟨r⟩ = 0.477);
  diagnosis: XX is free — many-body spectrum equals subset-sums of
  single-particle energies to 1.3e-14, so the Poisson heuristic for
  *generic* integrable systems does not apply. **Spec §8 Amendment 2**
  (pre-run): certificate replaced by free-spectrum reconstruction < 1e-8 —
  strictly stronger. PASS under amended certificate;
- localized (XXZ W = 8, Sz = 0): ⟨r⟩ = 0.382 ∈ [0.36, 0.42] — PASS;
- DTC (ε = 0.03): subharmonic peak present 20/20 (mean power fraction
  0.936) — PASS.
Symmetry-sector handling recorded (reflection-even for uniform Ising;
Sz sector for particle-conserving chains).

**Phase 3 — calibration pilot (COMPLETE; spec §8 Amendment 3).** Manifest
(seeds, grids, windows) fixed and committed BEFORE execution. Pilot ran
per-group in the background (first full-pilot launch killed by a command
timeout before any results were consumed; restarted per-group — no data
seen, no preregistration impact). 7 groups × 5 runs at N = 10; ~2 min per
group. Findings (full record in spec §8 Amendment 3 and
results/AR-010/pilot_summary.json):
- witness battery separates all groups at baseline (PR_A ordering + Ξ);
  W1 binning cannot resolve the metastable doublet (Ξ carries that class);
- dephasing response monotone and class-ordered, with *negative* log ρ for
  quasiperiodic/metastable (dephasing pins oscillating Φ to its mean — an
  early sustained-by signature); quench null for all dynamical classes;
- ε_Φ = 0.05 shown to lie below the −log construction's finite-size noise
  floor (chaotic baselines 0.11–0.22; cap diagnostic confirms genuine
  fluctuation, not cap noise) — stationarity was unattainable at N = 10.

**Owner rulings (in-session):** ε_Φ → 0.25 (Amendment 3.1); confirmatory
strengths λ = 0.1, γ = 0.01, overriding the analysis script's mechanical
grid-edge pick with recorded reasoning (Amendment 3.2).

**Next phase (not this session): confirmatory runs** — full ensembles
(20/class), both system sizes, all witnesses + comparators + switch-off +
subsystem loss, T-B rigidity curves, criterion (a)/(b) statistics per the
amended spec. AR-019 window remains open until those runs begin.

## Delta list

(pending)

## ADR candidates raised

(pending)

## Open items

(pending)
